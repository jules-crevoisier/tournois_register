# Database Configuration
# ======================
# For local development, you can use Prisma's local database or your own PostgreSQL instance
# For production (Vercel), use Vercel Postgres connection string
DATABASE_URL="postgresql://username:password@localhost:5432/tournament_db"

# Payload CMS Configuration
# =========================
# Secret key for Payload CMS (generate a random string)
# Generate with: openssl rand -base64 32
PAYLOAD_SECRET="your-secret-key-here-minimum-32-characters"

# Server URL (important for Payload CMS)
# For local: http://localhost:3000
# For production: https://your-app.vercel.app
NEXT_PUBLIC_SERVER_URL="http://localhost:3000"

# Vercel Blob Storage (Optional for file uploads)
# ===============================================
# Create a Vercel Blob storage in your Vercel dashboard
# and copy the read/write token here
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxxxxxxxxx"

# Node Environment
# ================
# Automatically set by Vercel, but you can override for local development
# NODE_ENV="development"
